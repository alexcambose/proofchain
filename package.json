{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^5.1.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "dev": "lerna run --parallel dev",
    "prepare": "husky install"
  }
}
